version: '3'

services:
  uptime_kuma:
    image: louislam/uptime-kuma
    container_name: uptime_kuma
    env_file: uptime_kuma.env
    ports:
      - 3001:3001
    volumes:
      - ./data:/app/data
    restart: unless-stopped
