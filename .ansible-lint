use_default_rules: true

verbosity: 1

skip_list:
  - git-latest
  - package-latest
  - no-relative-paths
  - internal-error
  - name[casing]

exclude_paths:
  - '$HOME/.ansible/roles'
  - files/docker_compose
